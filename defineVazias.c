// Tipo abstrato de dados "Tabuleiro"

// Retorna celula vazia, ou seja, com um zero
Celula[] defineVazias(Tabuleiro){
    int matriz[9][9];
    for(int i = 0; i < 9; i++){
        for(int j = 0; j < 9; j++){
            if(matriz[i][j] == 0){
                // retorna a celula vazia;
            }
        }
    }
}